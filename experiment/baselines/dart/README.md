# DART Baseline Archive

This folder keeps the legacy DART baseline files for reference only.

The main repository entry points (`train.py`, `evaluate.py`, `configs/model/kasm.yaml`)
are KASM-only and do not import this baseline. If you want to revive the DART
baseline, treat this folder as archived source material and wire it into a
separate baseline package.
