# Retriever

Reserved for retrieval code.

When implemented, this module should write sentence-level retrieved knowledge as
JSONL files compatible with `experiment/dataset/<dataset>/{train,dev,test}.jsonl`.
The current `experiment/` module does not include retriever implementation code.
