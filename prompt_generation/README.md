# Prompt Generation

Reserved for prompt-generation code.

The KASM training/evaluation pipeline is under `experiment/` and consumes the
retrieved JSONL files already present in `experiment/dataset/`.
